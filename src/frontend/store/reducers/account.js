let initialState = {
    username: undefined,
    role: undefined,
    workspaces: [],
    workspace: undefined,
    workspaceName: undefined,
    loaded: false
};
  
const account = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default account